import './Hero.css';

function Hero() {
    return (
        <header className="hero">
            <h1 className="hero__title">Curated Archive</h1>
            <p className="hero__tagline">A list of React projects built whilst learning to use the framework.</p>
        </header>
    )
}

export default Hero;